import { FilterMedPipe } from './filter-med.pipe';

describe('FilterMedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMedPipe();
    expect(pipe).toBeTruthy();
  });
});
