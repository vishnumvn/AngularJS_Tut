import { InnerTextDirective } from './inner-text.directive';

describe('InnerTextDirective', () => {
  it('should create an instance', () => {
    const directive = new InnerTextDirective();
    expect(directive).toBeTruthy();
  });
});
